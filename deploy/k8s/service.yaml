apiVersion: v1
kind: Service
metadata:
  name: auth-api
  namespace: auth
  labels:
    app.kubernetes.io/name: auth-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-api
  ports:
    - name: http
      port: 80
      targetPort: http
